# Simple Calculator – Dockerized

This repository contains a simple Python-based calculator application that can be run locally or inside a Docker container.
Docker repo link: https://hub.docker.com/r/navish1508/imt2023060

---

## 🚀 Run Using Docker

### **1. Pull the Docker Image**

```bash
docker pull navish1508/imt2023060:tagname
```

### 2. To start the application:

```bash
docker run -it navish1508/imt2023060:tagname
```
